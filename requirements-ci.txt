# Core dependencies for CI testing
numpy>=1.21.0
opencv-python>=4.5.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
Pillow>=8.0.0

# YOLO object detection (includes torch dependencies)
ultralytics>=8.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.0.0

# Note: CLIP is installed separately from GitHub to avoid version conflicts